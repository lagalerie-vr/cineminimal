'use client';

import React from 'react';
import ProfileView from '@/components/ProfileView';

/** The signed-in user's own profile. Same surface as /u/[username]. */
export default function ProfilePage() {
  return <ProfileView />;
}
